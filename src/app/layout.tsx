import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";
import Navbar from "@/components/features/navbar";
import { ThemeProvider } from "next-themes";

const customFont = localFont({
  src: [
    {
      path: "../../public/font/TerminessNerdFontMono-Regular.ttf",
      style: "normal",
      weight: "400",
    },
    {
      path: "../../public/font/TerminessNerdFontMono-Italic.ttf",
      style: "italic",
      weight: "400",
    },
    {
      path: "../../public/font/TerminessNerdFontMono-Bold.ttf",
      style: "bold",
      weight: "400",
    },
  ],
  variable: "--font-custom",
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${customFont.variable} antialiased`}>
        <ThemeProvider attribute="class" defaultTheme="dark">
          <Navbar />
          <div className="px-10 pb-5">{children}</div>
        </ThemeProvider>
      </body>
    </html>
  );
}
